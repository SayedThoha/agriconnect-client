import { AppointmentCodePipe } from './appointment-code.pipe';

describe('AppointmentCodePipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentCodePipe();
    expect(pipe).toBeTruthy();
  });
});
